<view wx:if="{{ item }}" class="album-card" bindtap="onTab">
  <image mode="aspectFill" class="image" src="{{ item.cover.uri || '' }}"></image>
  <view class="title">{{ item.name }}</view>
  <view class="description">{{ item.description }}</view>
</view>
