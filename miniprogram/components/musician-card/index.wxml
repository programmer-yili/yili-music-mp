<view wx:if="{{ item }}" class="card" bindtap="onTab">
  <image mode="aspectFill" class="photo" src="{{ item.photo.uri }}"></image>
  <view class="name">{{ item.name }}</view>
  <view class="category">{{ item.remark }}</view>
</view>

